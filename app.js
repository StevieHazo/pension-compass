const pages=[...document.querySelectorAll('.page')],links=[...document.querySelectorAll('.top nav a')];
function route(){const name=(location.hash||'#home').slice(1);const page=pages.find(p=>p.dataset.page===name)||pages[0];pages.forEach(p=>p.classList.toggle('active',p===page));links.forEach(a=>a.classList.toggle('active',a.hash==='#'+page.dataset.page));mobile.hidden=true;menu.setAttribute('aria-expanded','false');scrollTo(0,0)}addEventListener('hashchange',route);route();
const menu=document.getElementById('menu'),mobile=document.getElementById('mobile');menu.onclick=()=>{mobile.hidden=!mobile.hidden;menu.setAttribute('aria-expanded',String(!mobile.hidden))};
const filterButtons=[...document.querySelectorAll('.filters button')],cards=[...document.querySelectorAll('.tool-card')];filterButtons.forEach(b=>b.onclick=()=>{filterButtons.forEach(x=>x.classList.toggle('active',x===b));cards.forEach(c=>c.hidden=b.dataset.filter!=='all'&&c.dataset.category!==b.dataset.filter)});
const gbp=new Intl.NumberFormat('en-GB',{style:'currency',currency:'GBP',maximumFractionDigits:2}),num=v=>Number(String(v).replace(/[^0-9.]/g,'')),fmt=v=>new Intl.NumberFormat('en-GB',{maximumFractionDigits:0}).format(v);const annual=document.getElementById('annual'),lump=document.getElementById('lump'),slider=document.getElementById('slider'),out=document.getElementById('lumpOut');slider.oninput=()=>{lump.value=fmt(+slider.value);out.value=gbp.format(+slider.value)};lump.oninput=()=>{const n=num(lump.value);if(Number.isFinite(n)){slider.value=Math.min(100000,n);out.value=gbp.format(n)}};[annual,lump].forEach(x=>x.onblur=()=>{const n=num(x.value);if(Number.isFinite(n))x.value=fmt(n)});
document.getElementById('calc').onsubmit=e=>{e.preventDefault();const a=num(annual.value),l=num(lump.value),ae=document.getElementById('annualError'),le=document.getElementById('lumpError');ae.textContent=le.textContent='';results.hidden=true;if(!Number.isFinite(a)||a<=0){ae.textContent='Enter an annual pension greater than £0.';annual.focus();return}if(!Number.isFinite(l)||l<0){le.textContent='Enter a lump sum of £0 or more.';lump.focus();return}const ex=l/12;if(ex>a){le.textContent=`That lump sum would require more pension than the ${gbp.format(a)} entered.`;lump.focus();return}const remain=a-ex;resAnnual.textContent=gbp.format(remain);resMonthly.textContent=`${gbp.format(remain/12)} per month`;resLump.textContent=gbp.format(l);resExchanged.textContent=gbp.format(ex);resStart.textContent=gbp.format(a);resBar.style.width=`${Math.max(0,remain/a*100)}%`;working.textContent=`${gbp.format(l)} ÷ 12 = ${gbp.format(ex)} annual pension exchanged. ${gbp.format(a)} - ${gbp.format(ex)} = ${gbp.format(remain)} annual pension remaining.`;results.hidden=false;results.scrollIntoView({behavior:'smooth'})};document.getElementById('print').onclick=()=>print();
